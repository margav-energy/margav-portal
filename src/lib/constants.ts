export const APP_NAME = "Margav Portal";
export const APP_VERSION = "0.1.0";
export const PAGE_SIZE = 5;

/**
 * Shown as a fallback "Get In Touch" contact on the quote document (see
 * src/components/esignature/QuoteSummarySidebarCard.tsx and
 * QuoteDocumentPreview.tsx) alongside the assigned rep's own number — for
 * whoever a customer reaches when the rep isn't available. Left unset
 * until there's a real number to show; the "Head Office" row only renders
 * when this is filled in, never a placeholder.
 */
export const HEAD_OFFICE_PHONE = "01889 256069";
export const HEAD_OFFICE_EMAIL = "";
